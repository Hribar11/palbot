# Create a bot at https://discord.com/developers/applications and paste its token here.
DISCORD_TOKEN=[CHANGEME]

# Optional, but recommended: your Discord server ID. Commands sync immediately to this server.
DISCORD_GUILD_ID=[CHANGEME]

# Role whose members may operate the Palworld server (case-sensitive) e.g. Palworld Admin.
ALLOWED_ROLE_NAME=[CHANGEME]

# Full path to the dedicated server executable on the Windows server. C:\PalworldServer\Pal\Binaries\Win64\PalServer-Win64-Shipping-Cmd.exe
PALSERVER_EXE=[CHANGEME]

# Working directory used when launching it. Usually the folder containing the executable. C:\PalworldServer\Pal\Binaries\Win64
PALSERVER_WORKING_DIR=[CHANGEME]

# Arguments passed to the server. Leave empty if none are needed.
PALSERVER_ARGS=-useperfthreads -NoAsyncLoadingThread -UseMultithreadForDS

# Seconds to wait for graceful termination before force-killing the process tree.
STOP_TIMEOUT_SECONDS=30

